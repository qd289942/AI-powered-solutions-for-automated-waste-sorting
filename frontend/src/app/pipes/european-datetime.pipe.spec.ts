import { EuropeanDatetimePipe } from './european-datetime.pipe';

describe('EuropeanDatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new EuropeanDatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
